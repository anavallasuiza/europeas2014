define([], function() {
	'use strict';
	var helpers = {
		percent: function(total, value) {
			return Math.floor((value * 100) / total);
		},
		logos: {
			'PP': 'Logo PP'
		
		}
	};

	return helpers;

});
